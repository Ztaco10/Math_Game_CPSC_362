import random

def spawn_zombie(file):
    return random.choice(file.readline())

# make a zombie attack if the player gets answer wrong

# make a zombie die if player answers certain amount of answers correct

# design each zombie with its own characteristic